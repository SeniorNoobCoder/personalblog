<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ecare.app.server.applog">

	<!-- 添加日志 -->
    <insert id="save" parameterType="pd">
	    INSERT INTO app_users_log
	    (id, user_id, username, type, content, path, createtime) 
	    VALUES (#{id}, #{user_id}, #{username}, #{type}, #{content}, #{path}, #{createtime});
    </insert>
	
	
</mapper>