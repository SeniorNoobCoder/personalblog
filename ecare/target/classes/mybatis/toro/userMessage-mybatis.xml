<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ecare.web.toro.userInfo">
    <!--保存通话记录-->
    <insert id="saveCallRecord" parameterType="pd">
        INSERT INTO customer_call_record (
            customer_sn,
            customer_id,
            create_time,
            callPhone,
            STATUS,
            remove_logo,
            operator_time
        )
        VALUES
        (
            #{customer_sn},
            #{customer_id},
            #{create_time},
            #{callPhone},
            '0',
            'N',
            NOW()
        )
    </insert>

    <!--保存设备进出入安全岛记录-->
    <insert id="saveInOutSafeArea" parameterType="pd">
        INSERT INTO customer_safearea (
            customer_sn,
            type,
            create_time,
            customer_id,
            status,
            update_time
        )
        VALUES
        (
            #{customer_sn},
            #{type},
            #{create_time},
            #{customer_id},
            'N',
            NOW()
        )
    </insert>
    <!--添加评价-->
    <insert id="saveAssessment" parameterType="pd">
        INSERT INTO customer_order_assessment (
        order_no,
        create_time,
        is_punctuality,
        service_quality,
        service_attitude,
        count
        )
        VALUES
        (
        #{order_no},
        #{create_time},
        #{is_punctuality},
        #{service_quality},
        #{service_attitude},
        #{count}
        )
    </insert>
    <select id="findAssessmentNum" parameterType="pd" resultType="Integer">
        SELECT
        COUNT(0) AS num
        FROM
        customer_order_assessment a
        WHERE 1=1
        <if test="order_no != null and order_no != '' ">
            AND a.order_no=#{order_no}
        </if>
    </select>
</mapper>